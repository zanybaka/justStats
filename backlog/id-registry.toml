# Backlog id registry (SKILL-028).
# Regenerated by the backlog-rotate skill, not maintained by hand.
# Regenerate: python .ai-task/scripts/backlog-id-registry.py update
# Helper missing? It is vendored, not tracked - run "ai-task init" to restore it.
# Per-prefix floors across active BACKLOG.md, backlog/archive/BACKLOG-*.md,
# and task-file names in backlog/tasks/ and backlog/archive/tasks/.
# Never allocate a new task id with a suffix <= max_seen for its prefix.

generated = "2026-08-01"
sources = ["BACKLOG.md", "backlog/tasks/ (filenames)"]

[prefixes.ACT]
max_seen = "ACT-003"
max_seen_num = 3
next_free = "ACT-004"
next_free_num = 4

[prefixes.ADR]
max_seen = "ADR-003"
max_seen_num = 3
next_free = "ADR-004"
next_free_num = 4

[prefixes.ASSET]
max_seen = "ASSET-001"
max_seen_num = 1
next_free = "ASSET-002"
next_free_num = 2

[prefixes.DEV]
max_seen = "DEV-001"
max_seen_num = 1
next_free = "DEV-002"
next_free_num = 2

[prefixes.DOC]
max_seen = "DOC-002"
max_seen_num = 2
next_free = "DOC-003"
next_free_num = 3

[prefixes.ICON]
max_seen = "ICON-005"
max_seen_num = 5
next_free = "ICON-006"
next_free_num = 6

[prefixes.INIT]
max_seen = "INIT-003"
max_seen_num = 3
next_free = "INIT-004"
next_free_num = 4

[prefixes.QA]
max_seen = "QA-004"
max_seen_num = 4
next_free = "QA-005"
next_free_num = 5

[prefixes.REL]
max_seen = "REL-006"
max_seen_num = 6
next_free = "REL-007"
next_free_num = 7

[prefixes.SCAN]
max_seen = "SCAN-008"
max_seen_num = 8
next_free = "SCAN-009"
next_free_num = 9

[prefixes.SET]
max_seen = "SET-004"
max_seen_num = 4
next_free = "SET-005"
next_free_num = 5

[prefixes.UPD]
max_seen = "UPD-004"
max_seen_num = 4
next_free = "UPD-005"
next_free_num = 5

[prefixes.UX]
max_seen = "UX-019"
max_seen_num = 19
next_free = "UX-020"
next_free_num = 20

[prefixes.VOL]
max_seen = "VOL-006"
max_seen_num = 6
next_free = "VOL-007"
next_free_num = 7
